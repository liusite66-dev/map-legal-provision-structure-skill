# Agent Notes

Use this Skill for user-supplied mainland-China normative legal texts that need a hierarchy tree or deterministic citation network. Obtain cloud privacy confirmation before reading inputs. Run `prepare`, read the index first, then run `report` with only the limited review JSON described in `references/analysis-schema.md`.

Never supplement material from the web, guess a reference target, merge versions, or label semantic legal relationships. See `SKILL.md` for commands and constraints.
